{
  "name": "widget-nan-deployment",
  "version": "1.0.0",
  "scripts": {
    "build": "cd widget && npm install && npm run build",
    "dev": "cd widget && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
