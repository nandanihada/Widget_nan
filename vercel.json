{
  "version": 2,
  "buildCommand": "cd widget && npm run build",
  "outputDirectory": "widget/dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/widget/dist/$1"
    }
  ]
}
